# Project Rules & Conventions

- JS/HTML/CSS only, no bundler.
- ≤500 LOC per file where reasonable; 2-space indent; LF line endings.
- Conventional commits (`feat:`, `fix:`, `docs:` …).
- Manual tests: import sample, change NIC count, randomize MACs, export & diff.
